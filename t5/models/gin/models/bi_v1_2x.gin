# -*-Python-*-
# bi_v1 but with ~2x the parameters and FLOPS
# Basically BERT-large with 16 layers per stack instead of 24

include 'models/bi_v1.gin'
include 'models/bi_bert_large.gin'

num_layers = 16
